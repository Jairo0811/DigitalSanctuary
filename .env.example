# Recommended production path: point the Android app to a backend/proxy that owns the Gemini credential.
AI_PROXY_URL=

# Optional local-development fallback only. Never commit a real key.
GEMINI_API_KEY=
